#include <iostream>

using namespace std;

int main(int argc, const char * argv[])
{
    int L, R;

    while(cin >> L >> R && (L != 0 && R != 0)){
    	cout << L+R << endl;
	}    
     
    return 0;
}